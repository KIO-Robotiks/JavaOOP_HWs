public class masks extends hygieneProduct{
    public masks(String name, Double price, Integer amount,
                          String unit, Integer amountInPackage) {
        super(name, price, amount, unit, amountInPackage);
    }
}
